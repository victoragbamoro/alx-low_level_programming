#include <stdio.h>

/**
 * main - Printing Alphabets in lower case
 *
 * Return:0
 *
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}
	putchart(10);
	return (0);
}
